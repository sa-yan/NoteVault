<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FirebaseApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize the colorPrimary attribute to change ActionBar background color -->
        <item name="colorPrimary">@color/backgroundColor</item>
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Customize the colorPrimaryDark attribute to change the status bar color -->
    </style>
</resources>